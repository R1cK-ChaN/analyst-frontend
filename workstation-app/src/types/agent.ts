// Agent message / stream / tool-call discriminated union.
// Concrete variants land in the next slice once the backend protocol is fixed.
export type AgentMessage = unknown
export type AgentStreamEvent = unknown
export type AgentToolCall = unknown
